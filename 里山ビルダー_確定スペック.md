# 里山ビルダー（勉強の木 v2）確定スペック

2026-07-24 に /grill-me で確定。「勉強の木」を Township/Hay Day 型の箱庭経営ゲームへ全面リニューアルする。

## コンセプト
勉強がコイン（通貨）を生み、昔の自然あふれる日本＝里山の村を育てる箱庭経営ゲーム。
「遊ぶ（村を前進させる）には勉強が必要」という構造を核に据える。

## 確定した設計判断

1. **進行エンジン = ハイブリッド**
   - 作物の生育など「時間で進む」部分は現実時間で進む（Hay Day のような待ち時間の中毒性を残す）。
   - 建設・購入・土地拡張など「前進」は勉強で得たコインが必須。

2. **通貨・資源構造 = コイン1種、勉強でのみ増える**
   - 通貨はコインのみ。コインは勉強でしか増えない。
   - 作物は現実時間で育つが、収穫物は「素材／建材」であってコインにはならない。
   - 建物・土地・種はコインが必須。建物はコイン＋素材で建つ。→ 勉強が常にボトルネック。

3. **稼ぎの差（整合性）= 集中にボーナス、手入力は減額**
   - タイマー通常 = 基本レート。
   - 集中モード完走 = ボーナス倍率（仮 ×1.5）。
   - 手入力 = 減額（仮 ×0.5、必要なら1日上限）。オフライン勉強を一応救いつつ、集中を経済的に得にする。

4. **ゲート = ソフト経済ゲート**
   - 村はいつでも眺められる。作物の収穫・出荷など既存資産の回収はいつでも可。
   - 建設・購入・拡張など「前進」だけが勉強コインを要求。罰的でなく自然。

5. **アート = 等尺（2.5D）手描き SVG（路線A＝コードで密度を突き詰める）**
   - 現行のフラット見下ろしを、斜め見下ろしの立体タイルへ進化。奈良・江戸の里山を和モダンに上品に。
   - コード生成・自己完結（外部アセット不要）。※ Hay Day の描き込みは再現不可、到達可能な高品質を狙う。
   - 2026-07-24 追記: ユーザーが里山集落の水彩イラストを提示し「これぐらい細かく」と要望。正直に「水彩の一枚絵の密度はコードのベクターでは不可能」「ゲームは部品が動く必要があり一枚絵とは両立しない」と説明した上で、①コードで密度を突き詰める(自己完結) ②描き込まれたスプライトPNG(参考質感に最も近いが素材用意/ライセンス/容量の負担、かつこの環境では画像生成不可) ③一枚絵を背景(ゲーム性と非両立) の3択を提示。**ユーザーは①(路線A)を選択**。
   - **【アート最終決定 2026-07-24】**: その後、密度追求 → 温かみ(夕方ゴールデンアワー)版 → ドット絵版 → 高解像度ドット絵版、と複数モックアップを見比べた結果、ユーザーは **「精査後(作り込み)」のベクター作り込みスタイルが一番良い**と決定。つまり **等尺(2.5D)ベクターSVG + 各要素を"作り込み基準"まで仕上げる** で確定。ドット絵路線は不採用。作り込み基準の内容=陰影(光源統一・面ごと3階調・接地影)、テクスチャ(藁の一本一本・葉の茂みの粒・木目・水の反射)、生活感(窓の橙色の灯り・薪の山・煙・暖簾)、シルエットの多様性。基準を示したモックアップは show_widget "craft_quality_before_after"(藁葺きの家と木の before/after)。温かみ(色温度)は"パレット設定"として実装時に調整可能にする(ユーザーは温かい方向も好む)。P1はこの作り込みベクターで組む。将来どうしても参考画像級のドット絵/水彩を狙うなら路線B(プロのスプライト素材パック)への移行余地は残す。

6. **ループの深さ = 浅め**
   - 作物 → 素材 → 建設。多段の加工チェーン（米→精米所→ごはん等）や注文は v1 では入れない（次段）。

7. **プレイヤーレベル = 累計勉強時間で上昇**
   - 既存 `growth.ts` のレベル計算を流用。レベルアップで新しい土地区画・新建物・新作物を解禁。
   - 土地拡張自体はコインで購入（＝レベルで権利が開き、コインで買う。二重に勉強ゲート）。

8. **画面構成 = 村が中心**
   - 下タブ: 村 / 勉強 / 図鑑・記録 / 設定。ショップは村画面内のボタンから。
   - 既存の継続機能（しおり・実行意図・渇いた畑など）は村の上に「お知らせ」として出す。

9. **勉強（集中）中 = 静かな村ビュー**
   - 村を薄く背景に見せ、コインがチャリンと貯まる演出＋集中の炎だけを控えめに。集中を妨げない範囲で動機づけ。

10. **MVP = 一周回る村 v1**
    - 等尺の村（数区画）、勉強→コイン、ショップで種と数種の建物・装飾、植える→現実時間で育つ→収穫で素材、
      素材＋コインで建設、レベルで土地拡張＋新要素解禁。既存の継続機能を村UIへ移植。

11. **データ引き継ぎ = 引き継ぐ（開拓ボーナス）**
    - 累計時間からレベルを算出（既存流用）＋過去の勉強に応じた控えめな初期コインを「開拓ボーナス」として付与。
    - 記録・ストリークも維持。

## 12. 成長ペースと「普請（ふしん）」システム【2026-07-31 確定】

### 背景（なぜ変えたか）
学習計画は英語＋数学で **合計約1200時間**。ところが旧設計では
- 実際に描かれた絵は12枚で、最後の絵（id11「御神木の里」）に **260pt＝約289時間** で到達
- その先の id12〜21 は同じ village_11.webp を使い回した仮ステージ

つまり **本当の最終形が全体の1/4の時間で見えてしまい、残り900時間ぶんは絵が1ミリも動かない**。
これでは後半のモチベーションが持たない、というのがユーザーからの指摘。

### 採った解決（絵は増やさず、絵と絵のあいだを割る）
新しい絵を大量生成する（Higgsfieldのクレジット・一貫性の問題で現実的でない）のではなく、
**隣り合う2枚の絵のあいだを「工程（Work）」に分割**して見せる方式にした。
隣の絵どうしは img2img で作られていて構図がほぼ一致しているため、
**次の絵の一部分だけを下から立ち上げる**と「土台を据える→柱を建てる→屋根を葺く」が実際に見える。

- 仮ステージ（id12〜21）は**廃止**。絵は12枚のまま。
- 12枚 → 11の遷移 → **全68工程**に分割。
- 工程内も1ptずつ連続で露出が進むので、**1回勉強するたびに必ず絵が動く**。
- 工程がひとつ終わるたびにお祝いが出る（旧: 12回 → 新: 68回）。

### 見せ方の3モード（`Work.mode`）
| mode | 見え方 | 使いどころ |
|---|---|---|
| `build` | region を**下から上へせり上げ**て露出 | 建物・田・橋。柱→屋根の普請感 |
| `grow` | region を**ふわっと重ねて**露出 | 木・桜・灯り・村人 |
| `dissolve` | 画面全体を**手前から奥へ幅広のぼかし帯で拭き取る** | 構図がずれている遷移（1→2, 2→3, 10→11）と各段階の締め |

※ 全体遷移に単純な opacity クロスフェードを使うと二重写しになるので、
斜めのワイプ（`to top right`・帯幅34%）にしている。「新しい土地が手前から開けていく」ように見える。

### ペース（1pt ≒ 1.11時間・集中モード換算）
1pt = 1000コイン、集中1時間 = 900コインなので **1pt ≒ 1.11時間**。

| 段階 | 名前 | 工程数 | 1工程 | 累計pt | 累計時間 |
|---|---|---|---|---|---|
| 1 | 最初の小屋 | 4 | 2pt | 8 | 約9h |
| 2 | 畑をひらく | 4 | 3pt | 20 | 約22h |
| 3 | 二軒目の家 | 4 | 4pt | 36 | 約40h |
| 4 | 井戸と田んぼ | 5 | 5pt | 61 | 約68h |
| 5 | 小道がのびる | 5 | 7pt | 96 | 約107h |
| 6 | 三軒目の家 | 7 | 8pt | 152 | 約169h |
| 7 | 蔵と水車 | 6 | 11pt | 218 | 約242h |
| 8 | 棚田の段 | 6 | 14pt | 302 | 約336h |
| 9 | 鳥居を仰ぐ | 7 | 18pt | 428 | 約476h |
| 10 | 実りの季節 | 8 | 22pt | 604 | 約671h |
| 11 | 御神木の里 | 12 | 25pt | **904** | **約1004h** |

- 最終形の到達が **約1000時間** ＝ 学習計画1200時間に対して約200時間のゆとり。
- 1工程あたりの時間は序盤 約2.2h → 終盤 約28h と自然に伸びる（工程内は1pt刻みで動くので止まって見えない）。

### 画面
- 「いま：柱を建てる（2/7工程）」＋工程バー＋「あと◯pt（約◯時間）」
- 段階内の工程ドット／段階の区切りを刻んだ「里山ぜんぶ」バー（満ちるまでの残り時間つき）
- 実装: `src/game/stages.ts`（工程表・`villageView()`）、`src/components/Village/VillageArt.tsx`（レイヤー描画）

### 残っている課題
- region（工程が露出させる矩形）は画像を見ての目分量。ずれが気になったら `stages.ts` の `R(...)` を調整する。
- （訂正）village_2/3/4 に見えていた黒い斑点は生成ノイズではなく、Readツールが透過部分をチェッカー柄で表示していただけだった。実際の合成描画（ブラウザ実機）で確認したところ問題なし。

## 13. 「本物の新規イラストで刻む」案の検討 → 保留、代わりに季節・時間帯フィルターを実装【2026-07-31/08-01】

### 背景
12番の普請システムをリリース後、ユーザーから「CSSの切り貼りではなく、各区間に本当に新しい絵をHiggsfieldで生成して刻んでほしい。特に10枚目→11枚目（実りの季節→御神木の里）は差が激しすぎる」という要望が来た。

### 実際に検証したこと
このセッションからHiggsfieldのMCPに直結していたため、想像でなく実測した。
- **残高: 24.25クレジット**（starterプラン）
- **画像1枚（1k, nano_banana_pro, image-to-image込み）＝2クレジット** → 残高で作れるのは最大12枚程度
- ローカルのvillage_10.webpを実際にHiggsfieldへアップロード→確認まで通し、導線自体は動くことを確認済み
- 動画（image-to-video）も試算: Seedance 2.0 Mini 4秒/480pが4クレジット、Kling 3.0 Turbo 5秒/720pが7.5クレジット、料金表の目玉のSeedance 2.0 std 5秒/720pは22.5クレジット（残高ほぼ全部）。Plusプラン(月1,200クレジット)は「動画を使えるようにする」のではなく「量を増やす」もの。

### 決定：今回は生成を保留
10→11の間に5〜8枚など具体的な案を提示したが、ユーザーは「まずは無料の代替案を聞きたい」を選択。動画のテスト生成も見送り。
→ **クレジットを使わない案から着手する**ことで合意。将来、10→11の橋渡しなど優先度の高い1箇所にだけ絞って少数生成する余地は残す（`build.py`等は `/private/tmp/.../scratchpad/preview_src/` に一時生成物として残っている。恒久保存はしていない）。

### 実装した無料施策：季節・時間帯フィルター
既存12枚の絵を増やさず、実機の時計・月からCSSフィルター＋色被せ(overlay, mix-blend-mode)を薄く重ねて「同じ絵でも今日は違う顔をしている」を作る。
- 時間帯: 朝焼け(5-8時) / 昼(8-16時、フィルターなし) / 夕方ゴールデンアワー(16-19時、暖色) / 夜(19-5時、暗く・青みがかった色被せ)
- 季節: 春(やや彩度up+微hue) / 夏(彩度・コントラストup) / 秋(セピア寄りの琥珀色) / 冬(彩度down+微寒色)
- 実装: `src/game/atmosphere.ts`（`atmosphereFor(date)`）、`src/components/Village/VillageArt.tsx` に `.vart-atmosphere` ラッパーとして統合。1分おきに時計を読み直す。`VillageArt` の `atmosphere` propで無効化も可能（現状はどの画面でも有効のまま）。
- ブラウザ実機で朝焼け/夜/秋の夕方の3パターンを確認済み（Dateをモンキーパッチして検証、本番コードには影響なし）。

### 保留中の他の無料施策案（未着手）
集中完走時のランダムな小イベント／軌跡タイムライン（過去に見た12段階を振り返るページ）。

## 14. 御朱印スタンプ＋節目の証明書【2026-08-01】

季節フィルターに続けて、無料施策から2つ実装。どちらも「段階(id1〜11)に到達する」ことをトリガーにしていて、データは共有している。

### 御朱印帳
- 段階(stages.tsのid1〜11、11個。id0はスタート地点なので対象外)に到達するたびに、朱色の丸いスタンプが1つ増える。
- ラベルは段階ごとの当て字2〜3字（初宿・開墾・並家・井田・通路・里道・蔵水・棚田・鳥居・実り・御神木）。`src/game/goshuin.ts` の `STAMP_LABEL` で管理。
- 表示は「記録」タブの最上部。未到達分は点線の丸プレースホルダー。
- 実装: `src/components/Records/GoshuinStamp.tsx`（SVG、stage idから決定的に歪ませた丸で手押し感を出す）、`src/components/Records/Goshuincho.tsx`（一覧・状態管理）。

### 節目の証明書
- 御朱印をタップすると、その段階の到達を記念する証明書をcanvasで描画して見せる。画像として端末に保存可能（`canvas.toBlob`→ダウンロード、追加ライブラリなし）。
- 内容: 村の絵(その段階の1枚)・段階名・到達日・到達時点の累計学習時間・朱色の落款(印)。
- 実装: `src/components/Records/Certificate.tsx`。

### データモデル
- `GameState.stampDates: Record<number, StampRecord>`（`StampRecord = { earnedAt: string; totalHours: number }`）を追加。`src/game/types.ts` / `src/game/state.ts`。
- 御朱印の発行は「里山ポイントが段階境界を跨いだ瞬間」に行う。寄進(`donateForGrowth`)が一度に複数段階を跨ぐ場合もまとめて発行する。`src/game/store.tsx`。
- **後追い付与**: この機能より前から遊んでいた人は、既に段階を超えているのにスタンプが無い状態になる。`GameProvider` マウント時に一度だけ、未記録の段階をその場で「今日到達したもの」として埋める（`totalHours`は現在の累計値で近似）。過去の実際の到達日は記録されていないための妥協。
- `game/store.tsx` は `src/store.tsx` の `useStore()`(累計学習時間 `totalSeconds`)に依存するようになった。Provider の入れ子順(`StoreProvider` → `GameProvider`)は `main.tsx` で既にこの順だったため変更不要。

## 15. 追加の無料施策4つ【2026-08-01】

10個出した案から「村人のひとりごと／週替わりお題／里の便り／あと少し通知」の4つを選定・実装。

### 村人のひとりごと
- 村画面を開くと、村人が小さな吹き出しで一言つぶやく。時間帯(atmosphere.tsのtimeBand)・ストリーク・直近に勉強した科目から候補を作り、日付+時間帯をシードに決定的に1つ選ぶ(1日の中では変わらない)。
- 実装: `src/game/villagerLines.ts`、`src/components/Village/VillagerChat.tsx`。`VillageScene` の村の絵のすぐ下に配置。

### 週替わりお題
- 科目名を決め打ちにできない(ユーザーが自由に科目を追加できる)ため、「今週の合計時間」「記録した日数」「集中モード完走回数」という科目非依存の3種類のお題を用意し、週番号のハッシュでローテーション。
- 合計時間チャレンジの目標値は「先週の実績×1.1(最低60分)」で動的に決める。達成でコイン+300(集中1約20分ぶん)。
- 実装: `src/game/weeklyChallenge.ts`(お題の決定・進捗計算)、`src/components/Home/WeeklyChallengeCard.tsx`。ホーム画面のVillageCardの下に配置。
- データモデル: `GameState.claimedChallengeWeeks: string[]`(週の開始日dateKeyで二重受け取り防止)。`game/store.tsx` に `claimWeeklyChallenge(weekKey, reward)` を追加。

### 里の便り
- 学習セッションを3回重ねるごとに、里にまつわる短い一文(全20話、`src/game/lore.ts` の `LORE_ENTRIES`)が1つ解禁される。永続データは持たず、`sessions.length` から解禁数を毎回計算するだけ(`unlockedLoreCount`)。
- 実装: `src/components/Records/VillageLore.tsx`。記録タブの御朱印帳の下、開閉式のカードとして配置。

### 「あと少し」後押しバナー
- 今の普請工程(`villageView().work`)が6割以上進んでいるときだけ、ホーム画面に「『◯◯』まで、あと○pt（約○時間）」というバナーを出す。工程が浅いうちは出さず、ゴールが近いときだけ後押しするツァイガルニク効果狙い。
- 実装: `src/components/Home/AlmostThereBanner.tsx`。DryFieldReminderの直下、VillageCardの上に配置。

### 共通の学び
- コイン表示は絵文字🪙ではなくSVGの`CoinIcon`コンポーネントを使うこと。環境によって🪙が色付きで描画されない(モノクロの丸に化ける)ことをこの実装中に確認したため。

## 実装デフォルト（未確定・調整可）
- 技術: React + Vite + TS 継続。SVG 等尺、DOM 配置タイル、ゲームエンジンなし。
- 保存: localStorage。ゲーム状態の JSON バックアップ（エクスポート/インポート）を追加。
- レート（config 化・要チューニング）: 基本 10 コイン/分、集中完走 ×1.5、手入力 ×0.5。→ 集中1時間 ≈ 900 コイン。
- 土地: 小さな区画から開始、レベルで数回拡張。
- 里山の初期コンテンツ: 田んぼ／畑（稲・大根・なす等）、建物（藁葺きの家・蔵・井戸・水車小屋・鳥居・石橋）、装飾（棚田・杉林等は現行流用）。

## 引き継ぐ既存機能（前セッションで実装済み）
集中モード（賭け）／終了演出（ピークエンド）→収穫演出に転用／しおりメモ／実行意図プランナー／渇いた畑リマインド／
週次ダイジェスト／フレッシュスタート／体調メーター／音・触覚フィードバック（ゲームSFXに拡張）。

## ビルド段階（フェーズ）
- P0: バックアップ済み。ゲーム状態のデータモデル（グリッド・タイル・在庫・コイン・建物・作物の植え付けをタイムスタンプ管理）＋勉強→コイン経済を既存セッション完了に配線。
- P1: 等尺の村レンダラ（SVG）＋数タイル＋タップで調べる。
- P2: ショップ＋植え付け＋現実時間の生育＋収穫→素材/在庫。
- P3: 建設（コイン＋素材）＋土地拡張＋レベル解禁。
- P4: 既存の継続機能を村UIへ移植＋静かな勉強ビュー。
- P5: アート磨き（等尺の陰影・里山パレット・アニメ）＋収穫演出＋統合テスト＋ビルド。
